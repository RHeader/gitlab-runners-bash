# gitlab-runners-bash
A versatile and fast way to deploy your gitlab runners on a remote machine, no need to write 'docker run and etc' every time 



Basic bash script scenario ./gitlab-docker-runners --token ' ' --url ' ' -- count 1-1000 or more 
